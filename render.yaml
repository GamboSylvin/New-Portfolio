services:
  - type: web
    name: blog-backend
    runtime: node   # yes, this looks weird but it's okay
    buildCommand: "./mvnw clean install"
    startCommand: "java -jar target/Blog-0.0.1-SNAPSHOT.jar"